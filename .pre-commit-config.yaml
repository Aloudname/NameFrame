repos:
  - repo: local
    hooks:
      - id: pytest
        name: pytest (phase 1)
        entry: /home/chenhaoran/projects/NameFrame/.venv/bin/python -m pytest tests/ -v
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-commit]
